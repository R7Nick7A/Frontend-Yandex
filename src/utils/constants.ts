// Базовый URL для API
export const API_URL = 'http://localhost:3000';

// Базовый URL для CDN
export const CDN_URL = 'http://localhost:3000';

// Селекторы для шаблонов
export const TEMPLATES = {
  CARD: '#card-catalog',
  CART: '#basket',
  MODAL: '#modal-container',
  ORDER: '#order',
  SUCCESS: '#success'
} as const;

// Селекторы для элементов
export const SELECTORS = {
  CATALOG: '#catalog',
  CART: '#basket',
  MODAL: '#modal-container',
  ORDER: '#order',
  SUCCESS: '#success'
} as const;

export const settings = {

};
